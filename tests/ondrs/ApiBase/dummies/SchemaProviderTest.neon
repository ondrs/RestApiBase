type: object
required: [message, code, params]
properties:
	message:
		type: string
	code:
		type: integer
	params:
		type: object
		required: [name, age, address]
		properties:
			name:
				type: string
			age:
				type: integer
			address:
				$ref: '#/definitions/nested.neon'
